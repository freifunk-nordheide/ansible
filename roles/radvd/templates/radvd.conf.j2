# {{ ansible_managed }}

interface br-{{ site_code }} {
  AdvSendAdvert on;
  IgnoreIfMissing on;
  AdvManagedFlag off;
  AdvOtherConfigFlag on;
  MaxRtrAdvInterval 200;
  AdvLinkMTU {{ (mtu | int) }};
  prefix {{ batman_ipv6 | ipaddr('network') | ipsubnet(64) }} {
    AdvOnLink on;
    AdvAutonomous on;
    AdvRouterAddr on;
  };
  RDNSS {{ batman_ipv6 | ipaddr('address')}} {
  };
};
